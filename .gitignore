# Runtime state & secrets — never commit
data/
**/data/
**/clients/
*.key
*.pem
*.conf
*-qr.png
tests/tmp/

# Local editor / OS
*.swp
.DS_Store

# Local Claude settings (personal, not shared)
.claude/settings.local.json

# Local design docs / superpowers artifacts (kept locally, not published)
docs/superpowers/
