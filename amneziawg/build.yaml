---
# NOTE: not consumed by CI (which builds via `docker buildx` with --build-arg
# BUILD_FROM) and ignored by Home Assistant when `image:` is set in config.yaml.
# Kept for local `docker build` convenience and documentation only.
build_from:
  aarch64: ghcr.io/hassio-addons/base:19.0.0
  amd64: ghcr.io/hassio-addons/base:19.0.0
args:
  AWG_GO_REF: v0.2.19
  AWG_TOOLS_REF: v1.0.20260618-2
labels:
  org.opencontainers.image.title: AmneziaWG Server
  org.opencontainers.image.source: https://github.com/kitos9112/hassio-addon-amneziawg
